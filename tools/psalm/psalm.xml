<?xml version="1.0"?>
<!--
    Psalm config lives under tools/psalm/, next to its isolated Psalm install. resolveFromConfigFile
    is off so the base directory stays the project root (where `composer psalm` runs), not this
    folder: projectFiles paths and the baseline's <file src> entries therefore remain root-relative
    and need no rewriting. errorBaseline is given relative to that root. The XSD in schemaLocation is
    resolved against this file's own directory (it is only used to validate the config).
-->
<psalm
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    resolveFromConfigFile="false"
    errorLevel="2"
    hoistConstants="true"
    findUnusedPsalmSuppress="false"
    findUnusedVariablesAndParams="true"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    ensureArrayStringOffsetsExist="true"
    addParamDefaultToDocblockType="true"
    errorBaseline="tools/psalm/psalm-baseline.xml"
>
    <projectFiles>
        <directory name="core" />
        <ignoreFiles>
            <directory name="vendor" />
            <directory name="core/Tokenizer" />
            <directory name="core/Output/JUnit" />
            <directory name="core/Output/Teamcity" />
            <directory name="core/Output/Terminal" />
        </ignoreFiles>
    </projectFiles>
    <issueHandlers>
        <MissingPureAnnotation errorLevel="suppress" />
        <MissingImmutableAnnotation errorLevel="suppress" />
        <TypeDoesNotContainType errorLevel="suppress" />
        <UnnecessaryVarAnnotation errorLevel="suppress" />
        <MissingInterfaceImmutableAnnotation errorLevel="info" />
        <MissingAbstractPureAnnotation errorLevel="info" />
    </issueHandlers>
</psalm>
